import styled from '@emotion/styled';

export const RecipesStyles = styled.div`
  display: grid;
`;

export const Recipe = styled.div`
  padding: 2rem;
  margin: 1rem 0;
  box-shadow: 0px 0.5px 5px 0px rgba(200, 200, 200, 1);
`;
