# import * from './generated/prisma.graphql'

type Query {
  recipes: [Recipe]!
}

type SuccessMessage {
  message: String!
}

type Mutation {
  createRecipe(
    name: String!
    ingredients: [String!]
    steps: [String!]
    notes: [String!]
  ): Recipe!

  deleteRecipe(id: ID!): SuccessMessage!
}
