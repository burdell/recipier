schema: http://localhost:4000/
documents: './components/**/*.graphql'
overwrite: true
generates:
  ../types/Graphql.tsx:
    config:
      noNamespaces: true
    plugins:
      - typescript-common
      - typescript-client
      - typescript-react-apollo
