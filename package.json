{
  "name": "recipier",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run --stream --parallel --scope={@recipier/api,@recipier/frontend} dev",
    "dev-frontend": "lerna run --scope={@recipier/frontend} dev",
    "dev-api": "lerna run --stream --parallel --scope=@recipier/api dev"
  },
  "keywords": [],
  "author": "nathan griffin <nathanjgriffin@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^2.11.0"
  },
  "prettier": {
    "singleQuote": true
  }
}
